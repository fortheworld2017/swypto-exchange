// Generated by CoffeeScript 1.10.0
(function() {
  var bind = function(fn, me){ return function(){ return fn.apply(me, arguments); }; },
    extend = function(child, parent) { for (var key in parent) { if (hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; },
    hasProp = {}.hasOwnProperty;

  App.PendingTransactionsView = (function(superClass) {
    extend(PendingTransactionsView, superClass);

    function PendingTransactionsView() {
      this.onPaymentSubmited = bind(this.onPaymentSubmited, this);
      this.onPaymentProcessed = bind(this.onPaymentProcessed, this);
      this.onTransactionUpdate = bind(this.onTransactionUpdate, this);
      return PendingTransactionsView.__super__.constructor.apply(this, arguments);
    }

    PendingTransactionsView.prototype.tpl = "pending-transaction-tpl";

    PendingTransactionsView.prototype.collection = null;

    PendingTransactionsView.prototype.payments = null;

    PendingTransactionsView.prototype.initialize = function(options) {
      if (options == null) {
        options = {};
      }
      this.payments = options.payments;
      if (options.hideOnEmpty) {
        this.hideOnEmpty = options.hideOnEmpty;
      }
      $.subscribe("payment-submited", this.onPaymentSubmited);
      $.subscribe("payment-processed", this.onPaymentProcessed);
      return $.subscribe("transaction-update", this.onTransactionUpdate);
    };

    PendingTransactionsView.prototype.render = function() {
      this.collection.fetch({
        success: (function(_this) {
          return function() {
            _this.collection.each(function(transaction) {
              var $existentTx, $tx;
              $tx = _this.template({
                transaction: transaction
              });
              $existentTx = _this.$("[data-id='" + transaction.id + "']");
              if (!$existentTx.length) {
                return _this.$el.append($tx);
              } else {
                return $existentTx.replaceWith($tx);
              }
            });
            if (_this.hideOnEmpty) {
              return _this.toggleVisible();
            }
          };
        })(this)
      });
      return this.payments.fetch({
        success: (function(_this) {
          return function() {
            _this.payments.each(function(payment) {
              var $existentPm, $pm;
              $pm = _this.template({
                payment: payment
              });
              $existentPm = _this.$("[data-id='" + payment.id + "']");
              if (!$existentPm.length) {
                return _this.$el.append($pm);
              } else {
                return $existentPm.replaceWith($pm);
              }
            });
            if (_this.hideOnEmpty) {
              return _this.toggleVisible();
            }
          };
        })(this)
      });
    };

    PendingTransactionsView.prototype.onTransactionUpdate = function(ev, transaction) {
      this.render();
      if (transaction.get("balance_loaded")) {
        return this.$("[data-id='" + transaction.id + "']").remove();
      }
    };

    PendingTransactionsView.prototype.onPaymentProcessed = function(ev, payment) {
      this.render();
      return this.$("[data-id='" + payment.id + "']").remove();
    };

    PendingTransactionsView.prototype.onPaymentSubmited = function(ev, payment) {
      return this.render();
    };

    return PendingTransactionsView;

  })(App.MasterView);

}).call(this);
